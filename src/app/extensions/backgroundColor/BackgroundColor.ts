import { createStyleMark } from "@/app/extensions/utils/createStyleMark";

export const BackgroundColor = createStyleMark({
  name: "backgroundColor",
  styleName: "background-color",
  commandName: "BackgroundColor",
});
